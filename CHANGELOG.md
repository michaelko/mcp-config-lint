# Changelog

## 0.1.2

- Fixed the README example finding count to match the current unsafe fixture output.
- Strengthened safe-example regression coverage to scan the documented example files directly.

## 0.1.1

- Added safe configuration pattern examples for pinned packages, pinned containers, narrow filesystem access, and HTTPS remote endpoints.
- Added regression coverage to keep safe examples free of high-severity findings.

## 0.1.0

- Initial release with MCP config discovery, JSON/JSONC parsing, security rules, text/JSON/SARIF output, and CI-ready exit thresholds.
